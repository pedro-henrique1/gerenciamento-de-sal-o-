services:

  db:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: gerenciador_salao
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: